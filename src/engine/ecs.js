export const mk_entity = state => {
    return state;
};

export const rm_entity = state => {
    return state;
};

export const mk_component = state => {
    return state;
};

export const mk_system = state => {
    return state;
};

export const mk_scene = state => {
    return state;
};

export const mk_current_scene = state => {
    return state;
};

export const mk_renderer = state => {
    return {
        type: 'renderer',
        opts: {
            aaa: 'fdafsa'
        }
    };
};

export const mk_tiles_from_tilemap = state => {
    return state;
};
